import 'package:flutter/material.dart';

class PlugOut extends StatelessWidget {
  const PlugOut({super.key});

  @override
  Widget build(BuildContext context) {
    return CircleAvatar(child: Text('.'),);
  }
}